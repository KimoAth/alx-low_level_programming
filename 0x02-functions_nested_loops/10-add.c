#include "main.h"
/**
 * add - function that adds two integers together
 * @n: takes in an integer
 * @m: takes in an integer
 * Return: k the result
 */
int add(int n, int m)
{
	int k = n+m;	
	return (k);
}
