#include "main.h"
#include <stdio.h>

/**
 * mul - make multiplication of two integers
 * @a: first integer
 * @b: second integer
 *
 * Return: the product of a and b
 */
int mul(int a, int b)
{
	int mul = a * b;
	
	return (mul);
}
